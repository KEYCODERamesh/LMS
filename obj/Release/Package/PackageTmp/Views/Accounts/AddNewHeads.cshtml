@model HospitalManagementSystem.AccHeadDT
@{
    ViewBag.Title = "AddNewHeads";
    Layout = "~/Views/Shared/_MainPageLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Add Test Panels</title>

</head>
<body>
    <div class="page-body">
        <!-- Container-fluid starts-->
        <div class="container-fluid">
            <div class="page-body-wrapper">
                <div class="container-fluid">

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
                            <hr />
                            <h4>Add New Accounts Heads</h4>
                            <hr />
                            <div class="form-group">
                                @Html.LabelFor(model => model.Acc_HeadName, "Head Name", htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-10">
                                    @Html.EditorFor(model => model.Acc_HeadName, new { htmlAttributes = new { @class = "form-control", placeholder = "ENTER ACCOUNT HEAD NAME" } })
                                    @Html.ValidationMessageFor(model => model.Acc_HeadName, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-offset-2 col-md-3">
                                    <input type="submit" value="Save" class="btn btn-outline-success-2x" />
                                </div>
                                <div class="col-md-offset-2 col-md-3">
                                    @Html.ActionLink("Add Accounts", "AddAccountHeadsList", new object { }, new { @class = "btn btn-outline-primary fa-plus-circlesssss" })
                                </div>
                            </div>

                            @*<div class="row">
                                    <div class="col-md-12">
                                        <table cellpadding="0" cellspacing="0" class="table table-striped table-bordered table-hover table-active">
                                            <tr>
                                                <th></th>
                                                <th>Group Name</th>

                                            </tr>
                                            @foreach (var item in Model)
                                            {
                                                <tr>
                                                    <td>
                                                        @Html.ActionLink("Edit", "EditGroups", new { id = item.AHID }) |
                                                        @Html.ActionLink("Delete", "Delete", new { id = item.AHID })
                                                    </td>
                                                    <td>@Html.DisplayFor(modelItem => item.Acc_HeadName)</td>


                                                </tr>
                                            }
                                        </table>

                                    </div>

                                </div>*@
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</body>
</html>